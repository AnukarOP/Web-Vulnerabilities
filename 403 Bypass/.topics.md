<!--
  Topic index for the 403 Bypass category of the Web Vulnerabilities Checklist.
  Repository: https://github.com/AnukarOP/Web-vulnerabilities
-->

# 403 Forbidden Bypass â€” Access Control Bypass Reference

This directory of the Web Vulnerabilities Checklist documents techniques to bypass HTTP 403 Forbidden responses, ACL filters, WAF deny rules and reverse-proxy path restrictions on web applications, APIs and admin interfaces. The full payload reference is in [403-bypass.md](./403-bypass.md).

## Synonyms

403 bypass, forbidden bypass, access control bypass, ACL bypass, WAF bypass, reverse proxy bypass, path normalization bypass, admin panel bypass, directory bypass, restricted endpoint bypass, HTTP 401 bypass, broken access control.

## Attack patterns covered

URL path manipulation, percent-encoding tricks (`%2e`, `%2f`, `%23`, `%3b`), double encoding, Unicode normalization, header injection (`X-Original-URL`, `X-Rewrite-URL`, `X-Forwarded-For`, `X-Custom-IP-Authorization`), HTTP method swap (`GET` â†’ `POST`, `OPTIONS`, `TRACE`), trailing slash variations, case manipulation, fragment bypass, Wayback Machine endpoint discovery, fuzzing for unprotected paths.

## Programs where 403 bypass pays

HackerOne, Bugcrowd, Intigriti, YesWeHack, Synack, Open Bug Bounty.

## Related OWASP categories

OWASP Top 10 A01:2021 â€” Broken Access Control Â· OWASP API Security Top 10 API1:2023 Broken Object Level Authorization Â· OWASP API5:2023 Broken Function Level Authorization.

## CWE references

CWE-284 Improper Access Control Â· CWE-285 Improper Authorization Â· CWE-862 Missing Authorization Â· CWE-863 Incorrect Authorization.

## Keywords

`403-bypass` `forbidden-bypass` `access-control-bypass` `acl-bypass` `waf-bypass` `path-traversal` `header-injection` `bug-bounty` `pentesting` `web-security` `appsec` `cwe-284` `cwe-862` `owasp-top-10` `hackerone` `bugcrowd` `intigriti` `oscp` `oswe`.

## See also

- Full checklist index: [../README.md](../README.md)
- Companion attack: [../Admin panal/adminpanal.md](../Admin%20panal/adminpanal.md)
- Companion attack: [../IDOR Vulnerability/idor.md](../IDOR%20Vulnerability/idor.md)
