<!--
  Topic index for the SQL Injection category of the Web Vulnerabilities Checklist.
  Repository: https://github.com/AnukarOP/Web-vulnerabilities
-->

# SQL Injection (SQLi) â€” Payload Arsenal Reference

This directory of the Web Vulnerabilities Checklist documents a comprehensive SQL injection payload library: error-based, time-based, UNION-based, boolean-blind, stacked queries, second-order, NoSQL injection (MongoDB, CouchDB) and database-specific payloads for MySQL, PostgreSQL, MSSQL, Oracle, SQLite. The full payload set is in [sqlpayload.md](./sqlpayload.md).

## Synonyms

SQL injection, SQLi, SQL injection payloads, blind SQL injection, time-based SQL injection, error-based SQL injection, UNION SQL injection, boolean-based SQLi, stacked queries, second-order SQLi, NoSQL injection, MongoDB injection, database injection, RDBMS injection.

## Attack patterns covered

Error-based SQLi (extract data via error messages â€” `extractvalue`, `updatexml`, `exp`, `geometrycollection`, `floor(rand())`), time-based blind SQLi (`SLEEP`, `WAITFOR DELAY`, `pg_sleep`, `BENCHMARK`), boolean blind SQLi, UNION-based SQLi (column count discovery, NULL probing, type matching), stacked queries (semicolon execution), second-order SQLi (injection stored and triggered later), authentication bypass (`' OR '1'='1`, `admin'--`, `' OR 1=1#`, `') OR ('1'='1`), out-of-band (OOB) via DNS exfiltration (`LOAD_FILE` on MySQL, `xp_dirtree` on MSSQL, `dblink` on PostgreSQL), file read / write (`LOAD_FILE`, `INTO OUTFILE`, `pg_read_server_files`), RCE pivots (MSSQL `xp_cmdshell`, PostgreSQL `COPY ... TO PROGRAM`, MySQL UDF), WAF bypass (comment-in-keyword `/*!50000UNION*/`, case alternation, unicode whitespace, scientific notation), NoSQL operator injection.

## Programs where SQLi pays

HackerOne, Bugcrowd, Intigriti, YesWeHack, Synack â€” SQLi remains one of the highest paid bug classes, particularly when leading to data dump, authentication bypass, or RCE.

## Related OWASP categories

OWASP Top 10 A03:2021 â€” Injection Â· OWASP API Security Top 10 API8:2023 Security Misconfiguration Â· OWASP ASVS V5 Validation, Sanitization and Encoding.

## CWE references

CWE-89 Improper Neutralization of Special Elements used in an SQL Command (SQL Injection) Â· CWE-564 SQL Injection: Hibernate Â· CWE-943 Improper Neutralization of Special Elements in Data Query Logic.

## Keywords

`sql-injection` `sqli` `blind-sqli` `time-based-sqli` `error-based-sqli` `union-sqli` `boolean-sqli` `stacked-queries` `second-order-sqli` `nosql-injection` `mongodb-injection` `mysql-sqli` `postgresql-sqli` `mssql-sqli` `oracle-sqli` `sqlite-sqli` `waf-bypass` `bug-bounty` `pentesting` `web-security` `appsec` `cwe-89` `owasp-top-10` `hackerone` `bugcrowd` `intigriti` `oscp` `oswe`.

## See also

- Full checklist index: [../README.md](../README.md)
- Companion attack: [../RCE/RCE.md](../RCE/RCE.md)
- Companion attack: [../Json Attack/json.md](../Json%20Attack/json.md)
- Companion attack: [../Admin panal/adminpanal.md](../Admin%20panal/adminpanal.md)
